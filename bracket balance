def is_balanced(value):
    """
    Функция для проверки сбалансированности скобок в строке.

    Аргументы:
    value -- строка, которую нужно проверить на сбалансированность скобок

    Возвращает:
    True, если скобки сбалансированы, иначе False
    """
    chek = 0  # Инициализация счетчика баланса скобок

    for element in value:
        if element == '(':  # Если встретилась открывающая скобка
            chek += 1
        elif element == ')':  # Если встретилась закрывающая скобка
            chek -= 1
        if chek < 0:  # Если баланс стал отрицательным, значит есть лишняя закрывающая скобка
            return False  # Возвращаем False и заканчиваем проверку

    return chek == 0  # Возвращаем True, если баланс скобок равен нулю

# Ввод строки от пользователя
value = input()

# Проверяем сбалансированность скобок и выводим результат
print('YES' if is_balanced(value) else 'NO')
